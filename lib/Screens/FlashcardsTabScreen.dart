import 'package:flutter/material.dart';

class FlashcardsTabScreen extends StatelessWidget {
  const FlashcardsTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Flashcards Page',
        style: TextStyle(fontSize: 24),
      ),
      
    );
  }
} 